# Bring Katas to the Browser

Project: Take katas, created for the CLI, and transform it into a program that can perform in the browser, while maintaining test coverage.

To run locally:

`cd <folder>`

`npm start`
